---
ha: "0.5.0"
object_type: "document"
creator: "1.1"
created: "2026-02-09"
status: "active"
visibility: "public"
flags: []
---

# 0.5.0 Master Object Schema

## Overview

Everything in the Hypernet is an Object. Objects are standardized containers that can hold any type of data—people, documents, devices, concepts, media, or physical items. This document defines the universal schema that all Objects share.

## Core Object Structure

```yaml
object:
  # ═══════════════════════════════════════════════════════════════
  # IDENTITY (Required for all Objects)
  # ═══════════════════════════════════════════════════════════════
  identity:
    address: "[Library Address]"          # e.g., "3.1.2.1.001"
    object_id: "[UUID]"                   # Globally unique identifier
    object_type: "[Type from 0.5.x]"      # e.g., "person", "document", "device"
    version: "[Semantic version]"         # e.g., "1.2.3"

  # ═══════════════════════════════════════════════════════════════
  # METADATA (Required for all Objects)
  # ═══════════════════════════════════════════════════════════════
  metadata:
    created:
      timestamp: "2025-01-30T12:00:00Z"
      by: "[Creator Mandala/Object]"
    modified:
      timestamp: "2025-01-30T14:30:00Z"
      by: "[Modifier Mandala/Object]"
    status: "active|deprecated|archived"
    visibility: "public|private|restricted"

  # ═══════════════════════════════════════════════════════════════
  # ACCESS CONTROL (Required for all Objects)
  # ═══════════════════════════════════════════════════════════════
  access:
    owner: "[Mandala ID]"
    permissions:
      read: "[Permission policy]"
      write: "[Permission policy]"
      delete: "[Permission policy]"
      share: "[Permission policy]"
    encryption:
      method: "AES-256-GCM"
      key_reference: "[Key envelope location]"

  # ═══════════════════════════════════════════════════════════════
  # CONTENT (Type-specific, defined in 0.5.x schemas)
  # ═══════════════════════════════════════════════════════════════
  content:
    # Varies by object_type
    # See specific schemas (0.5.1 - 0.5.9)

  # ═══════════════════════════════════════════════════════════════
  # LINKS (Relationships to other Objects)
  # ═══════════════════════════════════════════════════════════════
  links:
    - link_type: "[Type from 0.6.x]"
      target: "[Target Object address]"
      metadata: {}
    # ... additional links

  # ═══════════════════════════════════════════════════════════════
  # PROVENANCE (History and verification)
  # ═══════════════════════════════════════════════════════════════
  provenance:
    origin:
      source: "[Where this came from]"
      verification: "[How it was verified]"
    history:
      - version: "1.0.0"
        timestamp: "2025-01-30T12:00:00Z"
        change: "Created"
        by: "[Mandala ID]"
    signatures:
      - signer: "[Mandala ID]"
        signature: "[Cryptographic signature]"
        timestamp: "2025-01-30T12:00:00Z"
```

## Object Types Hierarchy

```
Object (Base)
├── 0.5.1 Person
├── 0.5.2 Organization
│   ├── Business
│   ├── Government
│   ├── Non-Profit
│   └── Community
├── 0.5.3 Document
│   ├── Text
│   ├── Structured Data
│   └── Code
├── 0.5.4 Media
│   ├── Image
│   ├── Audio
│   ├── Video
│   └── 3D Model
├── 0.5.5 Device
│   ├── Computing
│   ├── IoT
│   └── Sensor
├── 0.5.6 Location
│   ├── Physical
│   └── Virtual
├── 0.5.7 Event
│   ├── Meeting
│   ├── Transaction
│   └── Occurrence
├── 0.5.8 Concept
│   ├── Category
│   ├── Tag
│   └── Relationship Type
└── 0.5.9 Task
    ├── Action Item
    ├── Project
    └── Milestone
```

## Object Lifecycle

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│ Created │────►│ Active  │────►│Deprecated│────►│Archived │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                     │                               │
                     └───────────────────────────────┘
                           (Can be restored)
```

## Storage Model

Objects are stored as:
1. **Metadata chunk**: Always accessible, contains identity and access info
2. **Content chunks**: May be split across multiple storage nodes
3. **Link index**: Enables fast relationship traversal

```
┌─────────────────────────────────────────┐
│           Object Container               │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐   │
│  │     Metadata Chunk (always)      │   │
│  │  - Identity                      │   │
│  │  - Access control                │   │
│  │  - Link references               │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │     Content Chunk(s)             │   │
│  │  - Encrypted payload             │   │
│  │  - Split if large                │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │     Provenance Chunk             │   │
│  │  - Version history               │   │
│  │  - Signatures                    │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

## Validation Rules

All Objects must pass validation:

```yaml
validation:
  required_fields:
    - identity.address
    - identity.object_id
    - identity.object_type
    - metadata.created.timestamp
    - metadata.created.by
    - access.owner

  type_constraints:
    address: "valid_library_address"
    object_id: "uuid_v4"
    timestamps: "iso8601"
    mandala_refs: "valid_mandala_id"

  content_validation:
    method: "type_specific_schema"
    schema_location: "0.5.[type_number]"
```

## Related Documents

- 0.0.0 Library Addressing System
- 0.5.1 Person Object Schema
- 0.5.2 Organization Object Schema
- 0.5.3 Document Object Schema
- 0.6.0 Link Definitions Overview
- 0.8.1 Encryption Standards
