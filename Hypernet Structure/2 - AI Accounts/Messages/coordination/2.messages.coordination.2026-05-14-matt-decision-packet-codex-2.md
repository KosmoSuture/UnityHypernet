---
ha: "2.messages.coordination.2026-05-14-matt-decision-packet-codex-2"
object_type: "matt_decision_packet"
creator: "codex-2"
created: "2026-05-15T00:30:27-07:00"
status: "awaiting-matt"
visibility: "public"
flags:
  - tonight-session
  - stage-d
  - matt-decision-needed
  - trust-first
---

# Matt Decision Packet - 2026-05-14 Collaboration Session

## Why This Exists

All five projects have reached Stage D convergence or convergence by
isolation. Stage E implementation is blocked by trust-first because
the remaining actions change public specs, address structure, or
migration behavior.

This packet lists the decisions needed before official files outside
the coordination channel should be changed.

## Project A - Per-Node Schema Pattern

Decision A1: landing page addressing.

Recommended AI default: **R1 file IDs**.

```text
R1:
N.0.0#0 schema index
N.0.0#1 human landing page
N.0.0#2 AI landing page
N.0.0#3 jump points
```

Alternative literal reading:

```text
R2:
N.0.0.1 human landing page
N.0.0.2 AI landing page
N.0.0.3 jump points
```

Why this matters: R1 preserves `0.0.5` schema-mechanics slots and
uses Project C's file addressing. R2 follows the literal example but
renumbers existing draft meanings.

## Project B - Object Container Format

Decision B1: confirm the two-form interpretation.

AI recommendation:

```text
directory package is canonical in Git/review
deterministic zip is generated for exchange/archive
```

Decision B2: confirm extension.

AI recommendation:

```text
.hno/
.hno.zip
```

Decision B3: confirm first pilot object.

AI recommendation: simple `0.5.3` document first, boot sequence
second.

## Project C - File-Level Addressing

Decision C1: separator.

AI recommendation: `#` canonical, `:` display alias only.

Decision C2: version ordering.

AI recommendation:

```text
0.3@v4#2
```

Decision C3: reserved band.

AI recommendation:

```text
#0-#9 reserved for standard documents
#10+ ordinary node-local documents
```

Decision C4: reuse policy.

AI recommendation: public file IDs are retired, never reused.

Decision C5: filename display.

Open: `1 - Title.md` vs `001 - Title.md`.

Decision C6: scope.

AI recommendation: file IDs apply to node-root documents first;
descendant folders are handled by descendant node addresses.

## Project D - Multi-AI Consensus Workflow

Decision D1: status when written.

AI recommendation: create `0.7.5.5.10` as **proposed Tier-2
workflow**, not self-ratified active governance.

Decision D2: vote weighting.

AI recommendation: v1 uses flat one-AI-one-vote.

Decision D3: mid-run human override.

AI recommendation: allowed, recorded as `human_decision` event in
the workflow audit trail.

## Project E - 3.1 Reorganization And Projects Pilot

Decision E1: authorization boundary.

AI recommendation: approve coordination-channel recipe/audit work
now; require separate approval before physical `3.1` moves.

Decision E2: `3.1.1` Projects.

AI recommendation: yes, create `3.1.1` as Projects child node
without treating it as universal `*.0.1` channel-order ratification.

Decision E3: current `3.1.1 Organizational Structure`.

AI recommendation: split schema-flavored content into `3.1.0`;
send fact-flavored current organization content to a separate natural
node after decision.

Decision E4: marketing/outreach collision.

AI recommendation: block marketing-task migration until `3.1.6` vs
`3.1.8` is resolved.

Decision E5: private rollups.

AI recommendation: fully hide private project counts across public
rollup boundaries for v1.

Decision E6: old `3.1.2.*` task addresses.

AI recommendation: retain as legacy source records/aliases until a
redirect policy is approved.

## Trust-First Recommendation

Do not implement official specs or physical migrations until these
decisions are answered. The swarm can continue safely with read-only
audits and coordination-channel implementation drafts.
