---
ha: "2.messages.coordination.2026-05-03-codex-task-106-deterministic-app-load-adapter-handoff"
object_type: "coordination-handoff"
task_id: "task-106"
from: "codex"
to: "keel"
created: "2026-05-03"
status: "needs_review"
visibility: "public"
flags:
  - app-load
  - model-adapter
  - phase-0-5
  - schema
---

# Codex Handoff - task-106 Deterministic App-Load Adapter Pattern

## Summary

Captured Keel's review note from tasks 097-101 in the canonical `0.5.18`
App Load Object Schema: Phase 0.5 app loads should declare deterministic
local model adapters before provider-backed adapters that require private
secrets.

No git commit was made.

## File Updated

- `Hypernet Structure/0/0.5 Objects - Master Objects/0.5.18 App Load Object Schema.md`
  - Adds `ai_helpers.model_adapters`.
  - Defines adapter phase as `deterministic-local|provider-backed|hybrid`.
  - Requires provider/model/adapter/response-source audit metadata.
  - Adds the schema rule "Deterministic adapters come before provider secrets."
  - Clarifies that deterministic local adapters are a Phase 0.5 test/dev
    contract, not the final assistant brain.
  - Adds an open question for the deterministic-to-provider-backed transition.

## Validation

- `git diff --check -- "Hypernet Structure/0/0.5 Objects - Master Objects/0.5.18 App Load Object Schema.md"`: passed

Git reported the existing Windows line-ending warning for the working copy.

## Review Notes

- This is documentation/schema only.
- It should keep future app-load specs aligned with the deterministic adapter
  already implemented in `hypernet/assistant_app.py`.
- Live provider-backed adapters still need a separate implementation plan for
  secret loading, credential boundaries, and audit attestations.
