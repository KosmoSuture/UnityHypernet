---
ha: 2.messages.coordination.2026-05-15-project-b-container-reference-impl-codex-1
type: coordination-note
status: posted
from: Codex-1
role: engineer-implementer
project: B
timestamp: 2026-05-15T01:24:24-07:00
---

# Project B Container Reference Implementation - Codex-1

## Trust Preflight

No commits. No destructive actions. This implementation adds the smallest runnable substrate for the Matt-locked `.hno` / `.hno.zip` decision and does not perform a pilot object conversion without swarm/Matt review.

## Files Changed

- `Hypernet Structure/0/0.1 - Hypernet Core/hypernet/container/__init__.py`
- `Hypernet Structure/0/0.1 - Hypernet Core/hypernet/container/manifest.py`
- `Hypernet Structure/0/0.1 - Hypernet Core/hypernet/container/validate.py`
- `Hypernet Structure/0/0.1 - Hypernet Core/hypernet/container/load.py`
- `Hypernet Structure/0/0.1 - Hypernet Core/hypernet/container/pack.py`
- `Hypernet Structure/0/0.1 - Hypernet Core/test_hypernet.py`

## Implemented Contract

| Area | Implemented |
|---|---|
| directory carrier | validates `.hno` directory form |
| zip carrier | validates `.hno.zip` transport before accepting it |
| required files | `manifest.yaml`, `schema.yaml`, `provenance/history.yaml`, `checksums.sha256` |
| required manifest sections | `container`, `identity`, `metadata`, `access`, `integrity`, `links`, `provenance` |
| checksums | SHA-256 manifest over every file except `checksums.sha256` |
| hostile zip checks | traversal, absolute paths, backslashes, duplicates, symlinks, entry count, total size, per-entry size, compression ratio, excessive depth, overlong components |
| deterministic packing | lexical member order, fixed timestamps, normalized permissions, deflate compression |

## Intentional Alpha Limit

`manifest.yaml` is parsed as JSON-compatible YAML only. JSON is a YAML subset and keeps the reference implementation dependency-free until the swarm chooses a full YAML parser.

## Validation

- `python -m py_compile hypernet/container/*.py test_hypernet.py` passed.
- Targeted container test passed.
- Full core test runner passed: `108 passed, 0 failed`.
- `git diff --check` passed for touched files, with only the repository's existing CRLF warning for `test_hypernet.py`.

## Handoff

Ready for peer review. Next runnable step, if approved, is a small pilot `.hno` conversion for the selected `0.5.3` document, using this reference implementation as the acceptance check.
