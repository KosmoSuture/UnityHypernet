---
ha: "2.7.29.STAGE-B-RESUMPTION-BOOT-PROMPT"
object_type: "resumption_boot_prompt"
creator: "1.1.10.1.keel"
created: "2026-06-02"
status: "LAUNCHED-AUTO-RESTART"
visibility: "public"
flags:
  - g2-revise-remediation
  - stage-b-resumption
  - claude-resume-existing-session
  - auto-restart-permission
in_response_to:
  - "20260602T093000Z-codex-G2-INDEPENDENT-ACCEPTANCE-REVISE-7f3c9a2d.md (the G.2 verdict)"
  - "2.7.29.PROTO-PROMPT-v1-FINAL.md (the original spec proto-ML must satisfy)"
  - "_genesis-session/CONTINUATION-PACKET.md (the proto-ML's own resume notes)"
resumes_session: "401dd34a-8f7f-4d4e-a61d-f82d86d8e352"
permission_authority: "[[feedback-auto-restart-permission]] — Matt granted auto-restart for previously-approved sessions on 2026-06-02; this is a remediation reboot of the originally-approved proto-ML genesis (no scope expansion)"
---

# 2.7.29 — Stage B Resumption Boot Prompt (G.2 REVISE Remediation)

*Authored by Keel (1.1.10.1) 2026-06-02 after Codex's G.2 REVISE verdict.*

**Why this exists:** the proto-Master-Librarian (session `401dd34a`) reached the G.2 gate cleanly. Codex's cross-model independent review returned **REVISE** (not BLOCK, not ACCEPT). The proto-ML did not name itself, design a team, or spawn — it correctly stopped. The fixes are scoped Stage-B corrections, not a redo.

**Authority for this restart:** Matt granted automatic restart permission for previously-approved sessions on 2026-06-02 (see [[feedback-auto-restart-permission]]). This is a *remediation* of the original Matt-approved launch, with NO scope expansion: same instance, same NODE 0 authorization, same tool set, same gate discipline. Re-using the original session ID preserves continuity and avoids re-doing absorption work that was already correct.

## The Resume Message (what `claude --resume` sees)

The full resume message body is in `_genesis-session/RESUME-MESSAGE-G2-REVISE.txt`. It directs the proto-ML to:

1. Read the Codex REVISE verdict in full
2. Address the 6 remediation items from Codex's verdict
3. Reissue G.1 as a new coordination file (not overwrite the prior G.1)
4. STOP at G.2 again — do NOT name, do NOT design, do NOT proceed

## Orchestration Wrapper

A new wrapper `_genesis-session/resume-genesis-g2-revise.ps1` mirrors the original `run-genesis.ps1` discipline:
- Same model, tools, permission mode, MCP config, `--add-dir`, `--append-system-prompt`
- Uses `claude --resume <sid>` instead of fresh `-p`
- Auto-resumes on rate-limit (~1h sleep)
- Honors the `STOP` file kill-switch
- Logs to `stream.jsonl`/`stream.err`/`STATUS.txt` (appending to the existing genesis-session logs — same audit trail)

## What Happens Next

1. Wrapper launches in background → proto-ML resumes
2. Proto-ML reads REVISE verdict + works through the 6 items
3. Proto-ML reissues G.1 as a new coordination message (linking to REVISE verdict it addressed)
4. Proto-ML STOPS at G.2 again (auto-restart wrapper exits cleanly on exit code 0)
5. Keel relaunches the Codex G.2 reviewer (also auto-restart per [[feedback-auto-restart-permission]]) against the reissued G.1
6. New verdict → ACCEPT / REVISE / BLOCK → routed accordingly (Stage D requires fresh ACCEPT)

— Keel (1.1.10.1), 2026-06-02
