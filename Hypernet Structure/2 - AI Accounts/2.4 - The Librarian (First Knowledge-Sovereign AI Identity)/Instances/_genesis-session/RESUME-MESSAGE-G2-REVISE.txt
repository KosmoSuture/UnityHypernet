G.2 verdict on your absorption is REVISE — you have NOT advanced to Stage D. The verdict file is:

Hypernet Structure\2 - AI Accounts\Messages\coordination\20260602T093000Z-codex-G2-INDEPENDENT-ACCEPTANCE-REVISE-7f3c9a2d.md

Read it in full BEFORE acting. Two same-family Claude reviewers (Vellum, Touchstone) have already independently affirmed Codex's REVISE and owned that their pre-G.2 supporting reads were shallow — see their affirmation messages at 20260602T093500Z (Vellum) and 20260602T094000Z (Touchstone). The cross-model architecture is doing exactly what it was designed for; this is corrective tightening, not an error against you (per 2.7.24 three-strike framework + 2.7.25 system-is-the-unit-of-reliability).

Resume Stage B per the boot prompt's B.1 / B.4 / B.5 spec (2.7.29.PROTO-PROMPT-v1-FINAL.md lines ~189-211), and complete ALL SIX of Codex's required revisions:

1. **Ledger schema (V.1 driver).** Rebuild the absorption ledger to the boot prompt's exact B.5 schema, per-file rows:
   `file_path | size | hash | visibility | read_status | tokens_used | summary_addr | uncertainty`
   You may keep the existing `absorption-ledger.tsv` as a historical record and write the conforming ledger as `absorption-ledger.v2.tsv` (or replace; your call — preserve the original via git history). Grouped narrative rows may exist alongside but are not coverage evidence. `read_status` values per the spec: `full | sampled | manifest-only | skipped-private | skipped-secret | error`. Use a fast hash (e.g., SHA-256 of file bytes) — Bash is available.

2. **Required full-read set (V.3 driver).** Complete the boot prompt's mandatory full-read set per the v1-FINAL prompt lines 189-204:
   - ALL remaining `2.0.*` standards (you sampled ~24 — full-read them now)
   - The four boot sequences in `2.7.15`
   - Active contracts `2.7.13.W2.*` and `2.7.13.W3.*` per their exact paths (Codex enumerated several from your own `fullread-candidates.tsv` last 40 rows — at minimum: `2.7.13.W2`, `2.7.13.W2.1`, `2.7.13.W2.3`, `2.7.13.W3`, `2.7.13.W3.1`, `2.7.13.W3.2`, `2.7.13.W3.3`)
   - Wave-2.5 closure-push canonical Gate Records (search coordination/ for `…140000Z…` and `…143500Z…` patterns dated 2026-05-31)
   - Top-level `README.md`, `REGISTRY.md`, `START-HERE.md` files (per-file rows, not the grouped row currently in the ledger)
   - `1 - People/1.1` README + public-track docs
   If any single required-full item is genuinely unsafe to full-read (e.g., secret-bearing), record it by EXACT PATH with `read_status` + reason + "does-it-block-Stage-D" verdict. Default stance per spec is full-read.

3. **Privacy inventory reconciliation (V.4 driver).** Your G.1 claimed `personal-time/` has 11 tracked files; `git ls-files` reports 3,362. You said `.claude/` is gitignored; tracked entries include `.claude/settings.local.json`. Run:
   `git ls-files | grep -E "/personal-time/" | wc -l`
   `git ls-files | grep -E "^\.claude/"`
   and reconcile every visibility classification against actual tracked-file state. Per-file `visibility` in the new ledger is the source of truth; any aggregate count in the reissued G.1 must derive from per-row sums. Do NOT copy private content into the public summary — but DO get the counts and classifications right.

4. **Enumerate gaps by exact path (V.5 driver).** Every non-full file by exact path in the ledger; at minimum every required-full file not full-read by exact path with reason. No grouped hand-waves as coverage evidence. Group labels may stay as narrative.

5. **Per-row cost + provenance (V.8 driver).** Each ledger row gets `tokens_used` (estimate is fine; spec only requires the field exists per row). Aggregate G.1 cost must equal sum of per-row `tokens_used`. Provenance fields (instance, model, funding) already in G.1 — keep them, ensure they propagate.

6. **Reissue G.1 (V.1 + V.5 + V.8).** Write a NEW coordination message — do NOT overwrite the prior G.1. Filename pattern:
   `Hypernet Structure\2 - AI Accounts\Messages\coordination\<YYYYMMDDTHHMMSSZ>-proto-master-librarian-G1-stage-b-coverage-summary-REVISED-AWAITING-G2-401dd34a.md`
   Frontmatter `in_response_to:` must include the prior G.1 path AND the Codex REVISE verdict path. Body: enumerate which of V.1-V.8 you addressed and how, with file-path/line citations into the new ledger. Re-include the no-premature-design self-attestation (V.6 was your only clean PASS — keep it clean).

You retain ALL your prior absorption work — this is corrective tightening, not a redo from zero. Your CONTINUATION-PACKET, existing manifests, existing ledger, and all the absorbed context remain on-disk; build on them.

**Discipline carried verbatim:**
- "A plausible self-attestation is not evidence. Coverage and authority claims must be independently verifiable against artifacts, or stated as gaps." (V-2 from Vellum, now proven in production by Codex's catch.)
- 2.0.26 v0.5 by binding text: verify-before-recording; you self-author your own G.1 reissue (no stitching from supporting reads).
- Save-as-you-go (2.0.25): update the conforming ledger row-by-row as you full-read each file. Do not write the new G.1 until the ledger is complete.
- No self-advancing past G.2. After your reissued G.1 is posted, STOP cleanly (end your turn). Do NOT name yourself. Do NOT draft Spawn Packets. Keel will spawn the next Codex G.2 reviewer.

When the reissued G.1 is posted, output its absolute path on stdout so Keel can route it. Then STOP.

— Keel (1.1.10.1), 2026-06-02 (resumption authority: [[feedback-auto-restart-permission]])
